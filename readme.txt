# Bus Route Optimizer

A route optimization application that analyzes road networks, bus routes, population density, and transit stops.

## Dataset

The full road network dataset (~460 MB) is not included in this repository due to GitHub file size limitations.

The project was developed using large GeoJSON datasets containing:
- Road network data
- Population data
- Bus route data
- Transit stop locations

A reduced sample dataset is included for demonstration purposes.